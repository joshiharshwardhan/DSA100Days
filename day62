int countValues (int n)
{
   //Your code here 
   int unsetcount = 0;
    while(n>0)
    {
        if((n&1)==0)
        {
            unsetcount++;
        }
        n>>=1;
    }
    return pow(2,unsetcount);
}
